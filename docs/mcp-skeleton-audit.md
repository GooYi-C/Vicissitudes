# 骨架期遗留清单 ＋ VS-01 批次卡（mcp 审计 · 2026-09-17）

> **性质**：骨架期收官审计（主理人已拍板入仓，2026-09-17）。批次卡已入文档仓 §二十九.7（VS-01）；分派表补行见 §三十.1；拍板行见拍板汇总 2026-09-17。本文件为遗留清单全文的存档位（文档仓拍板行回指此处）。
> **依据**：2026-09-17 只读侦察（MCP 远程实跑 `pnpm gate` / `gate:graph` / `gate:mut` 全绿；代码仓 HEAD = `01e7f3d` R4 验收）。
> **书写口径**：批次卡照 §二十七.1 模板全字段；遗留清单逐条给「事实 → 影响 → 处置建议 → 去处」。

---

## 一、骨架期遗留清单（G-1 ～ G-7）

### G-1 LLM 链路上半段未被任何批次认领（**分派缺口，最高优先**）

- **事实**：`src/llm/` 不存在；全 `src/` 零 `fetch` 调用；prompt 组装（LL-12）、SSE 客户端、成本双计量（LL-17）客户端侧均无实现。SK-04 批次卡「范围外」写明 prompt 组装「属 §二十 LL-12，SK-06 之后」，但 SK-06/SK-07/R1～R4 无任何一张卡认领——**施工卷分派表存在结构性漏项**。下半段（`parser/blocks → authorize → sanitize → compiler → TurnRunner`）已全部落地并有测试覆盖。
- **影响**：产品核心卖点（模型叙事＋意图落账）零通电；单回合真实成本、缓存命中率、意图遵循率三个决定内容期写法的数字全部未知。
- **处置**：**VS-01 批次卡认领**（见本文第二部分）。
- **去处**：入第五部新增节；§三十 分派表补一行（`LLM` 组客户端侧主责批次 = VS-01）。

### G-2 DayLog / 日结系统未落地，且与 §三十 分派表不符

- **事实**：附录 F 的 S-07（DayLog/DayFact）、S-08（日结边界）、S-09（叙事压缩链）、S-10（约定到期）零实现；存档现为整份快照（S-01 形状在，但「事实层」不存在）。§三十 分派表写明 `SAV` 组「日结／事实不灭」主责批次 = **R2**，R2 已标「已验收」——**这是四环中唯一一处验收状态与分派表内容不符的地方**。
- **影响**：①「压缩叙事、永不压缩事实」的记忆地基缺失，VS-01 通电后模型的长程上下文没有事实层可依；②报纸三阶注入（§12.5）、旧账链、传记终局全部以 DayLog 为前提。
- **处置**：立 **VS-02（DayLog 补账批次）**，排在 VS-01 之后、内容量产之前；拍板汇总已追加勘正行（R2 验收范围实际不含日结，诚实口径）。
- **补充侦察（起草 VS-02 时确认的代码现状边界）**：形状层已在——`saveSchema.ts` 有 `DayLog`/`DayFact`/`MonthLog` 接口与 `dayLogs`/`monthLogs`/`pastDigest` 字段（Zod `z.unknown()` 占位）、`saves.ts` 的 `autoSaves` 滚动保留（S-11 存储侧）已实现；生成层全缺——`turn/` 无日结判定、`events` 无约定到期扫描、月初快照未接线。VS-02 因此只补生成层与收紧形状，回滚零破档。
- **去处**：VS-02 批次卡**已立**（文档仓 §二十九.7）；拍板行已落。

### G-3 `tests/e2e` 为空目录，全部 e2e 命令靠 `--passWithNoTests` 放行

- **事实**：acceptance-r4.md 自己承认「tests/e2e 目录空，--passWithNoTests 放行（与 R2 同口径）」。骨架门 3/4/5（过月 12 次／脚本化完整回合／刷新存活）实为单测＋人工浏览器验收，无自动化回归。
- **影响**：`pnpm gate` 的绿灯覆盖面小于批次卡命令表的字面承诺；后续改动（尤其 VS-01 接线 App.vue）对骨架门 2/3/5 无回归保护。
- **处置**：最小代价方案——把 R1/R2/R4 已有的 loop 型单测（`r1-loop.spec` 等）升格补一份「门 3 复现」e2e（免 API 开局→12 月→存读档，jsdom 即可）；或显式改 §二十七.2 命令表，承认 e2e 延后并写明替代覆盖。**二选一，不许维持现状**（`--passWithNoTests` 是文档最痛恨的「恒真空转」的温和变体）。
- **去处**：并入 VS-01「命令」栏（该批次本来就要动 App.vue，顺手补门 3 e2e）或单独小批次。

### G-4 仓库无 git 远端（**单点风险，5 分钟可消除**）

- **事实**：`git remote -v` 为空，main 孤悬本机 D 盘。§3.2 第 0 层与 HANDOFF「第一天推远端」铁律未执行——而这条铁律的来源正是本项目「恢复目录单点丢失」的创伤史。
- **影响**：全部 8819 行源码＋五份验收记录处于单硬盘故障半径内。
- **处置**：**立刻推远端**（私有仓即可），不等任何批次。
- **去处**：无需批次卡；做完在拍板汇总补记一行即可。

### G-5 M-03 写域表事实上存在两份手写副本

- **事实**：`src/engine/registry.ts` 的 `M03_WRITER_CHAINS` 与 `src/parser/sanitize.ts` 的 `M03_REGISTRY` 是两个独立手写常量，靠注释宣称同源、靠人肉同步。LL-04 不变量 1「白名单由表生成，不手写第二份清单」在**文件间**层面未达成（各自文件内达成）。
- **影响**：加状态域／改写者链时的漂移温床——文档自己定义的「双份表必然漂移」。VS-02（DayLog 要加域）与 NPC 支柱（要加域）都会踩到这条缝。
- **处置**：把 M-03 登记表提为单一结构（建议住 L1 `validation/`，两处各自 import 投影），加一条断言锁两侧一致。**在下一次「加域」发生前完成**。
- **去处**：小批次或并入 VS-02 前置；建账一条（`MOD` 组追加）。

### G-6 E-4.4 史实锚定校准断言缺失

- **事实**：附录 E-4 第 4 条「五时代开局日各推 12 月，城池控制与时间线偏差 ≤2 座」无对应测试；`factions` 现行为「轨道模式」（`SIEGE_TARGETS=['guangzhou']`，攻城仅史实锚定城）＋守军均摊＋`BASE_INCOME=100` 初值。
- **影响**：轨道模式下此断言恒真（势力不自由扩张则永不偏离史实），**放开轨道的那一天没有回归保护**。
- **处置**：先写断言（现在就能过），再谈放开自由扩张——顺序不可反。势力放开本身属机制变更，走宪章层拍板。
- **去处**：断言建账（`NUM` 组或 `ECO` 组追加）；放开与否留内容期专题。

### G-7 已标注的骨架简化（登记即可，不必处置）

### G-8 账本副本同源仅存在性校验（VS-01 期间实际咬人，已处置 2026-09-17）

- **事实**：static-checks 原只查 `baseline/assertions.json` 存在，不查内容。VS-01 施工期间副本被改写（46,266B→19,557B）且 LLM-31/32/33 手写追加 `assertions-v2.json` 未跑 `--merge`，门禁未拦。
- **处置（已完成）**：① 文档仓 `ledger --merge` 入正账（176→179 条，LLM 组 33）；② 代码仓副本恢复逐字节同源（47,227B）；③ static-checks 升级为内容级校验（文档仓可达时逐字节比对，不可达退回存在性）——现 20 项。拍板行见文档仓拍板汇总 2026-09-17（与拍板B追认同行）。

| 项 | 现状 | 触发处置的条件 |
|---|---|---|
| 控制权省粒度 | `controllerForPolity` 按 provinceId 查 claim，「城为原子」未落地 | 地图专题重启时 |
| 终月判定 | `date.endsWith('-12')` 硬编码 | 日结规则落地（VS-02）时校准 |
| 势力守军均摊 | 势力总兵力 ÷ 控制城数 | 势力放开专题 |
| 现有 12 条事件全部无城市/身份门 | `when.cities:[]` | 内容量产第一步先给存量补门 |
| 内容填充度 | 事件 12/32、模板 8/19、时间线 29/58、世界书 12/36、地名/311 | 内容期主任务；量产前先建覆盖热力图工具 |

---

## 二、VS-01 批次卡

#### VS-01 LLM 垂直切片（补梁批次） ｜ 状态：未开工

- **目标（一句话，可验收）**：玩家在设置页填入自备 API key 后，自由输入一句话可走通「prompt 组装 → 同源 `/api/chat` SSE → 结构块解析 → 授权 → sanitize → 编译 → TurnRunner 原子提交 → 落档」全链并在 StoryView 看到流式叙事；免 API 路径行为逐位不变；产出四个实测数字（单回合成本 / 静态头缓存命中 / 意图块遵循率 / 提议可用率）记入验收文档。
- **前置**：`SK-07`、`R1`～`R4` 全部已验收（现状满足）；**G-4 远端已推**（不满足不开工——本批次改动面大，无远端不动工）。
- **产出文件**：
  - `src/llm/client.ts`（SSE 客户端：请求 `/api/chat`，消费 LL-19 错误分类码；含调用计数）
  - `src/llm/prompt.ts`（LL-12 最小实现：静态头段落顺序冻结＋模板哈希导出；动态段 = 世界状态投影＋可见处境注入＋变量树摘要。**记忆召回注入仅规则链——向量系统不在本批次**）
  - `src/llm/errors.ts`（LL-19 客户端统一错误表；`functions/api/chat.ts` 注释已预告此文件）
  - `src/stores/settings.ts`（修改：baseUrl / apiKey / model 本地设置区，S-04 口径——key 不入 SaveRecord）
  - `src/App.vue`＋`src/components/ChoiceInput.vue`（接线：有 API 配置时自由输入走 LLM 回合；无配置保持现行过月路径）
  - `src/turn/` 回合入口接线（模型输出经既有 `parseBlocks → authorize → sanitize → compileCommand → TurnRunner`——**下半管道零新建，只接线**）
  - `tests/unit/llm/{prompt,client,turn-loop}.test.ts`（mock 上游：坏块丢弃回合不中断／错误码分类／key 不出现在任何持久化产物）
  - `tests/e2e/gates/gate3-months.spec.ts`（顺手清偿 G-3 最小项：免 API 开局→12 月→存读档自动化）
  - `docs/acceptance-vs01.md`（含四个实测数字与 L3 步骤记录）
- **命令**：`pnpm gate` → `pnpm gate:graph`（新增 `src/llm/` 边须人工合法化登记）→ `wrangler pages dev` ＋ 真实 key 手工跑 **12 个真实回合**（L3，记录四数字）→ 免 API 回归：清空设置全流程重跑（调用计数必须 = 0）。
- **失败判据**：
  - key 出现在 SaveRecord / IndexedDB 存档区 / 日志 / prompt 快照 / 导出中（TEC-03 不变量 4、S-04）
  - 免 API 路径出现任意 ≥1 次网络调用（BIL-3 零调用地板回归——**本批次最大回归面**）
  - 静态头模板哈希跨回合不稳定（同设置两回合哈希不等即失败，LL-12／§6.7c）
  - 模型失败路径（超时/中断/4xx）改变世界状态（LL-19：降级不产生机制效果）
  - sanitize 出现第二份手写 AI 可写域清单（LL-04 不变量 1；注意 G-5 现状，本批次不得使其恶化为三份）
  - 同一模型输出文本两次进入回合管道产出不同 ops（下半管道确定性回归）
  - 前端出现对第三方域名的直连（TEC-03 不变量 2：同源代理唯一，embedding 例外不在本批次）
- **关联断言**：`LLM` 组（LL-10 消费侧／LL-12／LL-19）、`BIL` 组（零调用地板＋调用计数）、`SKL` 门 3（e2e 化）；新增条目与本批次同批写入 `baseline/assertions-v2.json`（§三十.2 纪律 2：建账与写码同批）。
- **完成宣称**：L2 ＋ **L3 必做**（12 个真实回合是人跑的现象，四个数字无 L3 不算数）。
- **范围外**（防顺手扩，逐条列死）：
  - extractor／memory 补写（LL-15）——compact 模式主补丁契约先行，兜底调用不做
  - embedding／向量召回（LL-18、§7.4b）——本批次记忆注入仅规则链
  - 意图识别器（LL-16，默认关，本批次不建）
  - split 调用模式（先只做 compact 默认档）
  - prompt 文案质感与内容圣经（内容期）
  - DayLog（VS-02）；报纸注入三阶（依赖 VS-02）
  - 开局 LLM 生成（§6.6：有 API 时的开局生成，后置）
- **回滚**：删 `src/llm/`、还原 `App.vue`/`ChoiceInput.vue`/`settings.ts` 三文件接线、还原 layer-graph 快照——下半管道与四环零触碰，回滚半径一目了然。

---

## 三、建议的执行顺序（收口）

```
① G-4 推远端（今天，5 分钟，不等任何批次）
② VS-01 补梁（上表）——通电 + 拿到四个数字
③ G-5 M-03 单源化（小批次，VS-02 的前置）
④ VS-02 DayLog 补账（待起草；含 G-2 拍板行 + 终月判定校准）
⑤ 内容期开工：存量 12 事件补门 → 覆盖热力图工具 → 量产管线
   （量产前按前几轮讨论决定：NPC 支柱是否升格，走宪章层拍板）
⑥ G-6 断言先行，势力放开与否届时专题
```
