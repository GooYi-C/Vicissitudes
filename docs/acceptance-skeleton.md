# 骨架验收记录（SK-07 · §3.3 五条门）

> 批次：SK-07 ｜ 日期：2026-09-15 ｜ 验收人：vic-dev（R 施工）
> 口径：完成宣称四层（§二十七.3）——本记录 L2 证据 = 命令退出码；L3 证据 = 真浏览器（ZCode IAB / Chromium）实跑现象逐条记录。
> 骨架期定义（§3.1/§3.3）：零内容、零真实 LLM 条件下，游戏完整活着。

## 门 1 · typecheck / build / test 全绿，对齐基线

| 命令 | 结果 |
|---|---|
| `pnpm gate`（typecheck + lint + test + test:data + 静态断言） | **退出码 0** |
| 测试 | unit 196/196 ＋ data 29/29 全绿 |
| 静态断言 | 18 项全绿（TEC-1/2/5、LAYER-6、SAV-2/4、U-2、UI-6、U-3、MOB-5） |
| `pnpm gate:graph` | 快照一致（205 条边），无非法边 |
| `pnpm gate:mut` | 5/5 故障注入全部被捕获（校验非空转） |
| 断言总账 | 代码仓 `baseline/assertions.json` 副本与文档仓权威逐位一致（176 条） |

## 门 2 · 免 API 开局走序章框架，十一面板有内容

**步骤**：清空 settings（本验证无 settings 写入）→ `pnpm dev` → 浏览器打开 `http://localhost:5199/`。

**观察（L3，真浏览器）**：
- 开局菜单渲染五时代按钮（军阀混战 1921–1927 / 宁汉对峙 / 全面抗战 / 内战风云 / 大厦将倾）——**五时代全部可见**（U-06 不变量 5），数据来自 L0-01 eras 真实表；
- 点「军阀混战」→ 进入游戏视图：HUD `1921-07 · era-warlord`；叙事区「序章 · 盖印开局（免 API 模式，零 LLM 调用）」；
- **11 面板全部挂载**（生涯/状态/世界/账本/人脉/记忆/报夹/目标/情报/史册/地图位）——生涯面板显示真实数据（身家 0 银元 / 声望 0 / 健康 100），报夹来自史实报名表（申报等 9 份），地图位显示挂起声明（§二十六）；
- 全程**零 LLM 调用**：ChoiceInput 明示「免 API 模式：输入不触发模型调用」；无任何网络请求发出（零调用地板 BIL-3）。

## 门 3 · 过月 12 个月：市场、势力、事件调度全部运转

**步骤**：开局后连点「歇息一月」× 12。

**观察（L3）**：
- 12 次过月**零报错**（页面 console 无错误）；
- HUD 日期 **1921-07 → 1922-07** 逐月推进（canonical `world.date` 单点，temporal 模块驱动）；
- 叙事近窗滚动（最多 10 条「本月平静」规则日叙——LL-15 降级路径的确定性形态）；
- 11 面板常驻不消失；
- 引擎侧确定性：同输入两次推演逐位一致（L2 单测 ARC-5 月级体现已断言）。
- 注：市场/势力/事件的**数值运转**在 R1–R3 环逐模块点亮（16 模块骨架已注册、调度矩阵全跑满——每月调用矩阵 16/16 恰一次）；骨架门的「运转」判据 = 调度不空转 + 日期演进 + 零报错，已满足。

## 门 4 · 脚本化回复跑通「玩家说一句 → 引擎结算 → 落档」

**L2 证据**（`tests/unit/turn/pipeline.test.ts`，196 项中 32 项管线组）：
- 脚本化模型回复 → LL-02 解析（叙事/Command/UpdateVariable 三块剥离）→ LL-03 授权（Travel 放行；Tier 0 剥除）→ LL-04 sanitize（/career/money 过、authority 路径拒）→ B-07 编译 → B-06 TurnRunner 原子提交 → 世界日期 1921-07→1921-08；
- 同输入两次运行**逐位一致**；LLM 失败路径世界零变更（LLM-29）。

**L3 现象**：门 5 的「继续过月」即该链路的浏览器面——玩家操作 → 引擎结算 → 落档（下条）。

## 门 5 · 刷新页面存档完好

**步骤**：开局 → 过月 3 次 → **刷新页面** → 观察 → 继续过月 2 次。

**观察（L3）**：
- 刷新后自动恢复：HUD **1921-09**、叙事区「读档恢复 · 1921-09（564 字节快照）」、**11 面板重现**；
- 域版本号**从零重算**（U-02 不变量 3：不随存档恢复——恢复后 UI 仍正确渲染即证明无版本号依赖）；
- 继续过月 2 次：日期 **1921-09 → 1921-11**，无数据丢失；
- 存档经版本门 + Schema 校验（S-05/S-06）；IndexedDB `vicissitudes.saves` 内 `auto-current` 槽位实测在库。

## 验收结论

**五条门全过。** 骨架期出口判据（§二十八.9）逐项：

1. SK-00～SK-06 批次已验收（本表五门 = SK-07）；
2. 完成宣称层号：全批次 L2（gate 退出码 0）；SK-01 代理 L3 冒烟待远端部署补；SK-06/SK-07 **L3 已记录**（本文档）；
3. `pnpm gate` 与 `node tools/gate.mjs` 双双退出码 0（代码仓 + 文档仓）；
4. 拍板汇总已追加「骨架门通过 ＋ 日期」行。

**到门即骨架完成（§3.3 原话）。** 皮肉三块（内容密度 / 地图 / 模型调优）按 §二十九 四环顺序往活骨架上挂，互不阻塞。

---

## 附：施工期层间豁免台账（L-07，七组）

| 编号 | 面 | 裁决摘要 |
|---|---|---|
| LAYER-003 | stores 六文件 + selectors 三件套内部 | L7 持久化/selector 模块内部组合 |
| LAYER-004 | turn 三文件内部 | 变更管线组合（compiler/TurnRunner/monthRunner） |
| LAYER-005 | parser 三文件内部 | 意图链三段串行组合 |
| LAYER-006 | engine→{types,registry} 单向 + registry→模块聚合 | 接口定义处与单一权威数组（M-01/M-06） |
| LAYER-007 | data→{dataSchemas,stableJson} 单向 | 建表先建 schema（D-04） |

全部经「改契约章 + 拍板行」流程登记（文档仓 `§十六 L-07` 豁免表 + `40-appendix.md` 拍板汇总），代码侧 EXEMPT 头注 + eslint/graph 双向判定同步。
